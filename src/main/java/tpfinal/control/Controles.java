package tpfinal.control;

/**
 * Clase que admnistra los controles. En este caso solo el teclado.
 * Pero podria tener en un futuro si decidieramos permitir jugar con el mouse una instancia del mouse.
 */
public abstract class Controles {
    public static final Teclado teclado = new Teclado();
}
